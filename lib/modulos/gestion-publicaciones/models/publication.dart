import 'dart:ui';

class Publication {
  int publicationId;
  int petId;
  String type;
  String name;
  String image;
  String comment;

  Publication(this.publicationId, this.petId, this.type, this.image, this.name, this.comment);

}