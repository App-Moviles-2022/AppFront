import 'dart:ui';

class Publication {
  String message;
  String title;
  String subtitle;
  String image;
  Color color;
  DateTime createdDate;

  Publication(this.message, this.image, this.createdDate, this.color, this.title, this.subtitle);

}